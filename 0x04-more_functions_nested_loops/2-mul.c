#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: init type number
 * @b: init type number
 * Return: the result of the 2 integers multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
